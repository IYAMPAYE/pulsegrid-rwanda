# Shared constants for filters, forms, and chart colors (match www/css/app.css :root)
regions   <- c("Kigali", "Huye", "Musanze", "Rubavu", "Nyagatare")
statuses  <- c("New", "In Progress", "Resolved")
urgencies <- c("Low", "Medium", "High")

REG_CHART_STATUS_COLORS <- c("#f59e0b", "#0058be", "#15803d", "#64748b")
REG_CHART_PRIMARY       <- "#0058be"
REG_URGENCY_COLORS      <- c("#dcfce7", "#fef9c3", "#fee2e2")
REG_SAFETY_ROW_COLOR    <- "#fef2f2"
